<Window x:Name="TinyHex" x:Class="TinyHex.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:TinyHex"
        Title="TinyHex" Height="600" Width="1200">
    <DockPanel>
        <Menu DockPanel.Dock="Top" HorizontalAlignment="Stretch">
            <MenuItem Header="_File">
                <MenuItem Header="_Open..." Click="MenuOpenFile_Click">
                    <MenuItem.Icon>
                        <Image Source="/Resources/OpenFile.png" />
                    </MenuItem.Icon>
                </MenuItem>
            </MenuItem>
        </Menu>

        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="5">
            <TextBlock Text="Bytes per line:" VerticalAlignment="Center" Margin="0,0,5,0" />
            <ComboBox x:Name="RecordSizeCombo" Width="80" SelectionChanged="RecordSizeCombo_SelectionChanged">
                <ComboBoxItem Content="8" />
                <ComboBoxItem Content="16" />
                <ComboBoxItem Content="32" IsSelected="True" />
                <ComboBoxItem Content="64" />
            </ComboBox>
        </StackPanel>

        <ListView x:Name="RecordListView"
                  ItemsSource="{Binding Records}"
                  VirtualizingStackPanel.IsVirtualizing="True"
                  VirtualizingStackPanel.VirtualizationMode="Recycling"
                  ScrollViewer.IsDeferredScrollingEnabled="False"
                  ScrollViewer.CanContentScroll="True"
                  FontFamily="Consolas"
                  FontSize="14" />
    </DockPanel>
</Window>
